import React from "react";

import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Series | Obslog",
  description: "blog series page",
};

const page = () => {
  return <div>series page</div>;
};

export default page;
